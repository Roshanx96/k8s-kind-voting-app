apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  type: clusterIP
  ports:
  - name: "vote-service"
    port: 8000
    targetPort: 80000
  selector:
    app: bjp-vote
    app: congress-vote
    app: aap-vote
    app: mkc-vote
  
