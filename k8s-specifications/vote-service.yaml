apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  type: NodePort
  ports:
  - name: "vote-service"
    port: 8000
    targetPort: 80000
    nodePort: 31002
  selector:
    app: cjp-vote
  
